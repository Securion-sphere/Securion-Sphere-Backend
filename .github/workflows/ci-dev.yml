name: CI for Development

on:
  workflow_dispatch:
  push:
    branches: ['dev']
  pull_request:
    branches: ['dev']

jobs:
  build-and-test:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      # Disable due to issue to pnpm via corepack
      # - name: Enable pnpm corepack
      #   run: corepack enable

      # - name: Prepare pnpm
      #   run: corepack prepare pnpm@latest --activate

      - name: Install and prepare pnpm
        run: npm i -g pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Lint NestJS application
        run: pnpm lint

      - name: Run Unit Test NestJS application
        run: pnpm test

      - name: Build NestJS application
        run: pnpm build
